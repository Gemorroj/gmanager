Файловый Менеджер + Текстовый Редактор
Gmanager

Версия: 0.7
Автор: Gemorroj
http://wapinet.ru

Номера кошельков. Надеюсь понятно для чего =)
WMR 167963845445
WMZ 225757696726



ПРАВА ДИРЕКТОРИИ В КОТОРОЙ ПРОИЗВОДЯТСЯ ОПЕРАЦИИ - 0777 !!!
Это можно сделать через FTP менеджер http://wapinet.ru/ftp/

Кодировка файла config.php и pattern.dat обязательно UTF-8 !

Если требуется перейти на директорию выше, в поле ввода пишите относительные, либо полные адреса, например, ../ или /home/user/public_html/ и т.д.
Так же, можно указывать путь к тебуемой папке в адресной строке, например, http://site.ru/manager/?../ или http://site.ru/manager/?/home/user/public_html/ и т.д.

При выборе кодировки в редакторе, в самом файле кодировка не меняется, а только выводится перекодированное содержимое файла.

Дизайн можно менять редактируя style.css

Сканер сайтов позволяет просматривать исходный код указанного Вами URL

C помощью EVAL можно выполнять PHP код и смотреть что этот код делает
Например, пишем
print 'Hello, world! =)';
Плучаем то, что сгенерировал интерпретатор, а именно - Hello, word! =)

При загрузке файла на сервер, в поле "Имя" можно написать директорию в которой будет сохранен загружаемый файл и имя этого файла, если имя не писать, то файл будет сохранен под его реальным именем.
Можно загружать несколько файлов сразу, для этого следует писать URL каждого нового файла с новой строки.
При импорте файлов, первым заголовком обязательно должен быть User-Agent

PHP Валидатор позволяет проверять синтаксис PHP кода

XML Валидатор проверяет файл на соответствие XML стандартам
WML и XHTML код обязательно должны соответствовать XML стандартам.
Будьте внимательны, если это скрипт, то проверяется то, что он сгенерировал, а не сам скрипт

Если НЕ указывать права на файлы/папки при их перемещении/копировании, то права изменяться не будут

Чтобы полноценно использовать возможности архиватора, в классе PclZIP присвойте константе PCLZIP_TEMPORARY_DIR значение - путь временной директории с правами на запись(pclzip.php 67 строка) Либо поставте права 777 на папку data/, по умолчанию константа установлена на эту директорию.

Работа с MySQL сервером происходит почти как в консоли =)

В файле pattern.dat несколько полезных шаблонов для SQL запросов, а так же шаблоны для вставки в создаваемые через менеджер файлы.

На хостингах, где по дефолту стоит PHP4, а PHP5 прикручен как CGI модуль, в .htaccess следует раскомментировать строку
#AddHandler application/x-httpd-php5 .php .php5 .phtml